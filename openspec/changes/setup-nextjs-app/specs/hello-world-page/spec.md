## ADDED Requirements

### Requirement: Home page renders Hello world
The application SHALL render a home page at the root path (`/`) that displays the text "Hello world" visibly on screen.

#### Scenario: Visiting the root path
- **WHEN** a user navigates to `/`
- **THEN** the page SHALL display the text "Hello world"
