## 1. Scaffold the Next.js Application

- [x] 1.1 Run `create-next-app` with TypeScript, Tailwind CSS, App Router, and ESLint enabled
- [x] 1.2 Verify the generated project structure (app directory, tsconfig.json, tailwind.config.ts, package.json)

## 2. Implement the Home Page

- [x] 2.1 Replace the default `app/page.tsx` content with a component that renders "Hello world"
- [x] 2.2 Strip out any boilerplate styles from `app/globals.css` that conflict with the minimal page

## 3. Verify

- [x] 3.1 Run `npm run dev` and confirm the home page at `http://localhost:3000` displays "Hello world"
- [x] 3.2 Run `npm run build` and confirm it completes without errors
